namespace Updater.IntegradorCRM.ApplyUpdater;

public class ApplyUpdate
{
    public void ApplyUpdate(string zipFilePath, string appPath)
    {
        string tempFolder = Path.Combine(Path.GetTempPath(), "update_temp");

        // Extrai os arquivos do ZIP
        ZipFile
    }
}
